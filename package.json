{
  "name": "textos-agent",
  "version": "0.1.0",
  "private": true,
  "description": "TextOS Agent backend — Cloudflare Worker + Anthropic Agent SDK + Hono.",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "types": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:models": "node scripts/check-model-access.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@supabase/supabase-js": "^2.45.0",
    "acorn": "^8.16.0",
    "hono": "^4.6.0",
    "jose": "^6.2.3",
    "mustache": "^4.2.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.25.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "@types/mustache": "^4.2.6",
    "typescript": "^5.6.0",
    "vitest": "^2.1.9",
    "wrangler": "^4.94.0"
  }
}
