node_modules/
.wrangler/
.dev.vars
.dev.vars.*
.env
.env.local
# bulk wrangler secret upload files — never commit
secrets-*.json
*.secrets.json
*.log
.DS_Store
dist/
worker-configuration.d.ts
.idea/
.vscode/
# scripts/ is a scratch drawer - 60+ one-off debug and diagnostic scripts.
# It stays ignored. The TrustLight end-to-end suites used to live here, which
# meant the only verification of the public API existed on one machine and in
# no commit; they are tracked under e2e/ instead.
scripts/
test-fetch.js

# scratch / debug scripts
fb.mjs

# local demo / customer preview files
docs/trustlight-south-louisiana-mission.html

# LeadScout exports — third-party prospect PII (business names, phone numbers,
# addresses). Read in place for the coldcall import; the data lives in Supabase.
# NEVER commit these.
victora_leads*.csv
*_COLUMN_MAP.csv
